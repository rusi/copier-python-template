[project]
name = "{{package_name}}"
version = "0.1.0"
description = "{{package_short_description}}"
authors = [
    {name = "{{full_name}}", email = "{{email}}"},
]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"
license = {text = "{{license}}"}

[dependency-groups]
dev = [
    "pytest<9.0.0,>=8.3.3",
    "pytest-mock>=3.14.0",
    "pytest-freezegun>=0.4.2",
    "pytest-cov>=6.0.0",
    "pydantic>=2.10.6",
    "mypy>=1.15.0",
    "coverage[toml]<8.0.0,>=7.6.1",
    "ruff<1.0.0,>=0.6.9",
]

[tool.pdm]
distribution = false

[tool.pdm.build]
includes = []
